# =============================================================
# RetailNova Enterprise Data Platform — Environment Config
# =============================================================
# Copy this to .env:  cp .env.example .env
# The setup.sh script creates .env automatically.
#
# IMPORTANT for Linux:
#   - When running scripts FROM the HOST machine → use "localhost"
#   - When running FROM INSIDE a container → use service name (e.g. "sqlserver")
#   - The Jupyter notebooks run inside a container, so they use service names
#     which are set via Docker environment variables in docker-compose.yml
# =============================================================

# ─── Environment ────────────────────────────────────────────
RETAILNOVA_ENV=dev          # dev | test | prod

# ─── SQL Server (Source — on-prem simulation) ────────────────
# Host-side: use localhost (port forwarded from container)
SQLSERVER_HOST=localhost
SQLSERVER_PORT=1433
SQLSERVER_DB=RetailNova_OLTP
SQLSERVER_USER=sa
SQLSERVER_PASS=RetailNova@2024

# ─── MinIO (Azure Data Lake Gen2 simulation) ─────────────────
# Host-side: use localhost
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=retailnova_admin
MINIO_SECRET_KEY=RetailNova@2024
MINIO_BUCKET=retailnova-dev

# ─── PostgreSQL Metadata Store ───────────────────────────────
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=retailnova_metadata
POSTGRES_USER=retailnova
POSTGRES_PASS=RetailNova@2024

# ─── Spark ───────────────────────────────────────────────────
# For scripts on HOST: local[*] (no cluster needed)
# For scripts in Jupyter: spark://spark:7077
SPARK_MASTER=local[*]

# ─── Alerts ──────────────────────────────────────────────────
ALERT_EMAIL=dataeng@retailnova.gr
